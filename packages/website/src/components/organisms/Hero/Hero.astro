---
interface Props {
  anchorId?: string;
  content?: string;
  cta?: CtaModel;
  subtitle?: string;
  title: string;
}

const { anchorId, content, cta, subtitle, title } = Astro.props;
---

<section
  id={anchorId}
  class="relative grid place-content-center place-items-center overflow-hidden py-[8vw]"
>
  <div class="container relative mx-auto w-full px-4 text-center">
    <h1 class="pb-5 text-5xl font-bold">{title}</h1>
    {subtitle && <p class="pb-5 text-lg font-medium">{subtitle}</p>}
    {content && <p class="mx-auto max-w-prose pb-8 text-lg">{content}</p>}
    {
      cta && (
        <a
          class="bg-primary-700 hover:bg-primary-600 inline-block scale-100 rounded py-3 px-5 text-base font-bold text-white subpixel-antialiased transition will-change-transform hover:scale-110"
          href={cta.url}
          target={cta.target}
        >
          {cta.text}
        </a>
      )
    }
  </div>
</section>
