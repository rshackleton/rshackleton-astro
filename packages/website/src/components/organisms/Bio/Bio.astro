---
interface Props {
  anchorId?: string;
}

const { anchorId } = Astro.props;
---

<section id={anchorId}>
  <p class="flex place-content-center place-items-center bg-neutral-800 px-8 py-16 text-center">
    TODO: Add bio here...
  </p>
</section>
