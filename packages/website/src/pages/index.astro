---
import Sections from '@/components/Sections.astro';
import Layout from '@/layouts/Layout.astro';
import { getHome } from '@/sanity/home';

const { sections, seo } = await getHome();
---

<Layout seo={seo}>
  <Sections sections={sections} />
</Layout>
